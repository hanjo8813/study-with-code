spring:
  batch:
    job:
      names: ${job.name:NONE}
    jdbc:
      initialize-schema: ALWAYS

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/mj_batch
    username: root
    password: 1234

  jpa:
    show-sql: true

  sql:
    init:
      mode: always